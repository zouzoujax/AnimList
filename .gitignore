node_modules/
out/
release/
dist/
.vite/
*.log
npm-debug.log*
.DS_Store
Thumbs.db
.env
.env.local

# Export GDPR personnel : contient jetons d'accès, IP, données de compte
DATA EXPORT/

# Outillage agent installé par `npx impeccable install` (2,9 Mo, 125 fichiers) :
# réinstallable en une commande, et settings.local.json est par convention local.
.claude/
.impeccable/

# Scripts de lancement locaux. « Create Realeses on Github.bat » contient un
# jeton GitHub en clair : ce fichier ne doit jamais partir dans le dépôt public.
*.bat
